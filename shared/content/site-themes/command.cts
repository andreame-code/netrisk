import type { SiteTheme } from "./types.cjs";

export const commandSiteTheme: Readonly<SiteTheme> = Object.freeze({
  id: "command",
  name: "Command",
  labelKey: "profile.preferences.theme.command"
});
