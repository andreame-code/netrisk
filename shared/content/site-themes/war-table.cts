import type { SiteTheme } from "./types.cjs";

export const warTableSiteTheme: Readonly<SiteTheme> = Object.freeze({
  id: "war-table",
  name: "War Table",
  labelKey: "profile.preferences.theme.warTable"
});
