import type { SiteTheme } from "./types.cjs";

export const midnightSiteTheme: Readonly<SiteTheme> = Object.freeze({
  id: "midnight",
  name: "Midnight",
  labelKey: "profile.preferences.theme.midnight"
});
