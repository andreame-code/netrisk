# Contributing

1. Describe your change and design intent.
2. Implement code and matching tests.
3. Run `npm run lint`, `npm run format:check` and `npm test`.
4. Update documentation in `docs/` when needed.
