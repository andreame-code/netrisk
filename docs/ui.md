# User Interface

The `ui` package provides a small Typer-based CLI for interacting with lobbies.
Commands:
- `lobby <id>`: create a lobby.
- `join <id> <player>`: join a lobby.
- `players <id>`: list lobby members.
